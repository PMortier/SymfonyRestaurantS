<!-- Sidebar/menu -->
<nav class="w3-sidebar w3-bar-block w3-white w3-collapse w3-top" style="z-index:3;width:250px" id="mySidebar">
	<div class="w3-container w3-display-container w3-padding-16">
		<i onclick="w3_close()" class="fa fa-remove w3-hide-large w3-button w3-display-topright"></i>
		<h3 class="w3-wide"><b>RestaurantS</b></h3>
	</div>
	<div class="w3-padding-64 w3-large w3-text-grey" style="font-weight:bold">
		{% if categories is defined %}
			{% for category in categories %}
				{# Affichage des différentes catégory #}
				<a onclick="myAccFunc('{{ category.name }}')" href="javascript:void(0)" class="w3-button w3-block w3-white w3-left-align" id="myBtn">{{ category.name }} <i class="fa fa-caret-down"></i></a>
				<div id="{{ category.name }}" class="w3-bar-block w3-hide w3-padding-large w3-medium">
					{% for restaurant in category.restaurants %}
						<a href="{{path('restaurant', { 'restaurantId' : restaurant.id } )}}" class="w3-bar-item w3-button"><i class="fa fa-caret-right w3-margin-right"></i>{{restaurant.name}}</a>
					{% endfor %}
				</div>
			{% endfor %}
		{% endif %}
	</div>
	<a href="#footer" class="w3-bar-item w3-button w3-padding">Contact</a>
	<a href="javascript:void(0)" class="w3-bar-item w3-button w3-padding" onclick="document.getElementById('newsletter').style.display='block'">Newsletter</a>
	<a href="#footer" class="w3-bar-item w3-button w3-padding">Subscribe</a>
</nav>
